# This should only be seen on the Local Version
# This line should not be here

# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
    adapter: mysql
    database: smhklaw_development
    username: root
    password:
    host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql
    database: smhklaw_test
    username: root
    password:
    host: localhost

production:
    adapter: mysql
    database: smhklaw_production
    username: root
    password:
    host: localhost
